#include <bits/stdc++.h>

#define mx 1000
#define mod 1000007
#define ll long long

using namespace std;

int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(false);
    int x, y, x1, y1;
    cin >> x >> y >> x1 >> y1;

    int n;
    cin >> n;
    int s;
    cin >> s;

    int disx = x1 - x;
    int disy = y1 - y;

    return 0;
}
